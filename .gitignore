# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/

# Environment
.env

# Evaluation results (raw and blinded — large, reproducible from blinded)
results/raw/
results/blinded/
results/pilot_scored.json
# results/v4_scored.json is committed (Session 4 output per BUILD_PLAN §Session 4)

# Chain mirror (large — MANIFEST.sha256 is committed instead)
data/v1_chains_mirror/*.jsonl

# Reference distributions (large — regenerate from chains)
data/*.pkl

# macOS
.DS_Store
